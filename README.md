# BONIPASTRIES — Local Preview

This folder contains a modernized static site for Bonipastries.

Preview locally:

```bash
# from the workspace root
python3 -m http.server 8000
# then open http://localhost:8000/BONITOR/Home.html
```

Notes and next steps:
- Replace `assets/images/*.svg` with high-quality photos (JPG/PNG).
- Update contact details in `contact.html`.
- Consider minifying CSS and bundling assets for production.
